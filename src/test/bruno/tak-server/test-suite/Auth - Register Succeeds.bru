meta {
  name: Auth Register Succeeds
  type: http
  seq: 5
}

post {
  url: {{url}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "TestPlayer123324",
    "email": "tbd@playtak.com"
  }
}

tests {
  test("success", () => {
    expect(res.getStatus()).to.equal(201);
  });
}
